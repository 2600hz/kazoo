{lib_dirs, ["../../deps"]}.
{deps_dir, ["../../deps"]}.

{erl_opts, [
    % native,
    {src_dirs, ["src", "plugins"]},
    debug_info, 
    fail_on_warning, 
    {parse_transform, lager_transform}
]}.

{cover_enabled, true}.
{cover_export_enabled, true}.

{deps, [
]}.
