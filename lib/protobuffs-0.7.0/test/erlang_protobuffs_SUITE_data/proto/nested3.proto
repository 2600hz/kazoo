message Outer {
  message Middle {
    message Inner {
        optional bool foo = 1;
    }
    optional Inner inner = 2;
    optional Other other = 3;
  }

  message Other {
      optional bool bar = 1;
  }

  optional Middle middle = 1;
}
