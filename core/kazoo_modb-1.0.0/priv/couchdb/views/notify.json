{
   "_id": "_design/notify",
   "language": "javascript",
   "views": {
       "smtp_log": {
           "map": "function(doc) {if (doc.pvt_deleted || doc.pvt_type != 'notify_smtp_log') return; emit([doc.pvt_created, doc.template_id], null);};"
       }
   }
}