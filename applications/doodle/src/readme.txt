this will be a pure store and forward messaging point for sms.

this shouldn't be doing any routing at all.
long term will be to send route_req for processin by callflow app
callflow should process and optionally send it for deliver by stepswitch
we could get some callflow actions like "reply"
