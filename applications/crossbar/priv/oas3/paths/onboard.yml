paths:
  /accounts/{ACCOUNT_ID}/onboard:
    put:
      parameters:
        - $ref: '../oas3-parameters.yml#/auth_token_header'
        - $ref: '../oas3-parameters.yml#/ACCOUNT_ID'
      requestBody:
        content:
          application/json:
            schema:
              $ref: '../oas3-schemas.yml#/onboard'
      responses:
        200:
          description: Successful operation
      summary: Add an instance of onboard
