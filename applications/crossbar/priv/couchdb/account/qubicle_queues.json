{
    "_id": "_design/qubicle_queues",
    "language": "javascript",
    "views": {
        "crossbar_listing": {
            "map": "function(doc) { if (doc.pvt_type != 'qubicle_queue' || doc.pvt_deleted) return; emit(doc._id, {'id': doc._id, 'name': doc.queue_name}); }"
        }
    }
}
