{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "_id": "tasks.billing.dump",
    "additionalProperties": true,
    "description": "Configuration data specifically for the billing.dump task",
    "properties": {
        "from_s": {
            "description": "Timestamp, in Gregorian seconds, for when to start the dump",
            "minimum": 62167219200,
            "type": "integer"
        },
        "store_csv": {
            "default": false,
            "descritpion": "Whether to store the generated CSV using the account's storage plan",
            "type": "boolean"
        },
        "to_s": {
            "description": "Timestamp, in Gregorian seconds, for when to stop the dump",
            "minimum": 62167219200,
            "type": "integer"
        }
    },
    "type": "object"
}
