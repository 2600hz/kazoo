{
    "_id": "_design/webhooks"
    ,"language": "javascript"
    ,"views": {
        "crossbar_listing": {
            "map": "function(doc) { if (doc.pvt_type != 'webhooks' || doc.pvt_deleted) return; emit([doc.pvt_account_id, doc._id], {'id': doc._id, 'hook': doc.hook, 'name':doc.name}); }"
        }
    }
}
