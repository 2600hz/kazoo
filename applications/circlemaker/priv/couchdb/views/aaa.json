{
  "_id": "_design/aaa",
  "language": "javascript",
  "views": {
    "fetch_doc": {
      "map": "function(doc) { if (doc.pvt_type != 'aaa' || doc.pvt_deleted) return; emit(doc._id, null); }"
    },
    "fetch_dicts": {
      "map": "function(doc) { if (doc.pvt_type != 'aaa_dict' || doc.pvt_deleted) return; emit(doc._id, null); }"
    }
  }
}
