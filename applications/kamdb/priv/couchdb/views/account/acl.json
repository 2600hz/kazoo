{
    "_id": "_design/acls"
    ,"language": "javascript"
    ,"views": {
        "crossbar_listing": {
            "map": "function(doc) { if ((doc.pvt_type != 'account' && doc.pvt_type != 'device') || doc.pvt_deleted || doc.acls == null) return; name = doc.realm || doc.sip.username; emit(name, {'type':doc.pvt_type, 'acls':doc.acls}); };"
        }
    }
}
