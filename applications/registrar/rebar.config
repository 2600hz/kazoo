{deps, [{lager, "3.2.1"}
       ]}.

{plugins, [{pc, "1.5.0"}
          ]}.

{port_specs, [{"priv/comp128.so", ["c_src/*.c"]}
             ]}.

{provider_hooks, [{pre, [{compile, {pc, compile}}
                        ,{clean, {pc, clean}}
                        ]}
                 ]}.
