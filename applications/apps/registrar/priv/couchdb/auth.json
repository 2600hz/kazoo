{ "_id":"_design/credentials"
  ,"language":"javascript"
  ,"views":{
    "lookup":{
      "map": "function(doc) { if ( typeof doc.sip == 'undefined') return; emit([doc.sip.realm, doc.sip.username], doc.sip); }"
    }
  }
}
