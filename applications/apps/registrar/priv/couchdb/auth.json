{ "_id":"_design/credentials"
  ,"language":"javascript"
  ,"views":{
    "lookup":{
      "map": "function(doc) { if (typeof doc.sip == 'undefined' || doc.pvt_deleted) return; var sip =JSON.parse(JSON.stringify(doc.sip)); sip.authorizing_id = doc._id; emit([doc.sip.realm, doc.sip.username], sip); }"
    }
  }
}
