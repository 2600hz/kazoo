{
    "_id": "_design/conferences"
    ,"views": {
        "listing_by_id": {
            "map": "function(doc) { if ( doc.pvt_type != 'conference') return; emit(doc._id, {'id': doc._id, 'name': doc.base.name}); }"
        }
        ,"listing_by_name": {
            "map": "function(doc) { if ( doc.pvt_type != 'conference') return; emit(doc.base.name,  {'id': doc._id, 'name': doc.base.name}); }"
        }       
    }
    ,"filters": {
        "export":"function(doc, req) { return ( doc.pvt_type == 'conference' ); }"
    }
}
