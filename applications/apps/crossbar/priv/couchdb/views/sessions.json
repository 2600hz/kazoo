{
    "_id": "_design/session",
    "views": {
        "expired_time": {
            "map": "function(doc) { if ( doc.session) emit(parseInt(doc.session[4])+parseInt(doc.session[5]), null); }"
        }
    }
}
