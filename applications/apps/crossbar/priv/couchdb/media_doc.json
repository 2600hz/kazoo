{
     "_id":"_design/media_doc"
    ,"language":"javascript"
    ,"views":{
        "listing":{
            "map":"function(doc) { if ( doc.pvt_type != 'media' ) return; emit(doc._id, {'display_name': doc.display_name, 'media_format': doc.media_format}); }"
        }
    }
}
