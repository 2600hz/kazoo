{ "_id":"_design/accounts"
  ,"language":"javascript"
  ,"views":{
     "listing":{
       "map":"function(doc) { emit(doc.acct_id, null); }"
       ,"reduce":"function(k, vs) { return true;}"
      }
    }
}

