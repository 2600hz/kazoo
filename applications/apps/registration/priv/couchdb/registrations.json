{ "_id":"_design/registrations"
  ,"language":"javascript"
  ,"views":{
    "get_contact":{
      "map": "function(doc) { var reg = doc.registrations; for ( i in doc.registrations ) { emit([ reg[i]['Realm'], reg[i]['Username'], parseInt(reg[i]['Event-Timestamp']) + parseInt(reg[i]['Expires'])], reg[i]['Contact']); } }"
      ,"reduce": "function(k, vs) { return vs; }"
    }
  }
}
