{
    "_id": "_design/agents"
    ,"language": "javascript"
    ,"views": {
        "crossbar_listing": {
            "map": "function(doc) { if (doc.pvt_type != 'agent' || doc.pvt_deleted) return; emit(doc._id, {'id': doc._id, 'name': doc.name, 'pin':doc.pin}); }"
        }
    }
}
