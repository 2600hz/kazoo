{
    "_id": "_design/hotdesks"
    ,"language": "javascript"
    ,"views": {
       "crossbar_listing" : {
           "map": "function(doc) { if (doc.pvt_deleted || !doc.hotdesk ) return; emit(doc._id, {'owner_id':doc._id, 'hotdesk': doc.hotdesk })}"
       }
    }
}
