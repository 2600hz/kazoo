{
    "_id": "_design/menus"
    ,"language": "javascript"
    ,"views": {
        "listing_by_id": {
            "map": "function(doc) { if (doc.pvt_type != 'menu' || doc.pvt_deleted) return; emit(doc._id, {'id': doc._id}); }"
        }
    }
    ,"filters": {
        "export":"function(doc, req) { return ( doc.pvt_type == 'vmbox' ); }"
    }
}
