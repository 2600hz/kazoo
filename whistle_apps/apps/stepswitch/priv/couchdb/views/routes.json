{
   "_id": "_design/routes",
   "language": "javascript",
   "views": {
       "listing_by_number": {
           "map": "function(doc) { if (doc.pvt_type != 'route' || doc.pvt_deleted) return; doc.numbers.forEach(function(num) { emit(num, null); }); }"
       }
   }
}
